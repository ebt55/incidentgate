# Proposer capture contracts

Each artifact in this directory is generated from the verified no-spend proposer
audit, committed, and frozen before its holdout capture. Artifacts record only
the admitted request contract and prompt hashes; they never contain provider
outputs.
