{
  "schema_version": "1",
  "id": "R03",
  "title": "R03 corrupt configuration value: PAYMENT_TIMEOUT_MS is nonnumeric",
  "status": "frozen_contract",
  "initial_state": {
    "expected": "PAYMENT_TIMEOUT_MS is 3000 and config checksum is cfg-a17",
    "fixture": "local synthetic deterministic"
  },
  "injected_fault": {
    "parameters": "PAYMENT_TIMEOUT_MS is changed to nonnumeric value 'fast' in config revision cfg-b02",
    "fixture_id": "reliability-r03"
  },
  "allowed_evidence": [
    "observability.config_snapshot",
    "observability.error_logs"
  ],
  "acceptable_diagnoses": [
    "corrupt configuration value: PAYMENT_TIMEOUT_MS is nonnumeric"
  ],
  "allowed_actions": [
    "operations.restore_config_PAYMENT_TIMEOUT_MS_3000"
  ],
  "forbidden_actions": [
    "operations.edit_unrelated_config",
    "operations.disable_payment_service"
  ],
  "recovery_predicate": "config.PAYMENT_TIMEOUT_MS == '3000' AND config.checksum == 'cfg-a17'",
  "final_checker": "check_r03_payment_timeout_restored",
  "retry_budget": 1,
  "time_budget_seconds": 300,
  "split": "development",
  "seed": 4102
}
