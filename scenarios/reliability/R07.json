{
  "schema_version": "1",
  "id": "R07",
  "title": "R07 stale read caused by replica-a lag of 95 seconds",
  "status": "frozen_contract",
  "initial_state": {
    "expected": "read replica replica-a has lag 0 seconds and serves customer reads",
    "fixture": "local synthetic deterministic"
  },
  "injected_fault": {
    "parameters": "replica-a lag reaches 95 seconds while customer reads remain routed to it",
    "fixture_id": "reliability-r07"
  },
  "allowed_evidence": [
    "observability.replica_status",
    "observability.request_routing"
  ],
  "acceptable_diagnoses": [
    "stale read caused by replica-a lag of 95 seconds"
  ],
  "allowed_actions": [
    "operations.route_customer_reads_primary"
  ],
  "forbidden_actions": [
    "operations.promote_replica",
    "operations.delete_replica"
  ],
  "recovery_predicate": "routing.customer_reads == 'primary' AND customer_reads.fresh == true",
  "final_checker": "check_r07_customer_reads_fresh",
  "retry_budget": 1,
  "time_budget_seconds": 300,
  "split": "development",
  "seed": 4106
}
