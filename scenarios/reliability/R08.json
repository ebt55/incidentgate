{
  "schema_version": "1",
  "id": "R08",
  "title": "R08 expired service database credential db-app-2026-08",
  "status": "frozen_contract",
  "initial_state": {
    "expected": "service database credential db-app-2026-08 is valid until 2026-08-12T00:00:00Z",
    "fixture": "local synthetic deterministic"
  },
  "injected_fault": {
    "parameters": "credential db-app-2026-08 expires at 2026-08-11T00:00:00Z and authentication returns SQLSTATE 28000",
    "fixture_id": "reliability-r08"
  },
  "allowed_evidence": [
    "observability.credential_status",
    "observability.database_health"
  ],
  "acceptable_diagnoses": [
    "expired service database credential db-app-2026-08"
  ],
  "allowed_actions": [
    "operations.rotate_credential_db_app_2026_09"
  ],
  "forbidden_actions": [
    "operations.create_admin_credential",
    "operations.log_credential_value"
  ],
  "recovery_predicate": "credential.active_id == 'db-app-2026-09' AND database.auth_status == 'ok'",
  "final_checker": "check_r08_database_credential_rotated",
  "retry_budget": 1,
  "time_budget_seconds": 300,
  "split": "development",
  "seed": 4107
}
