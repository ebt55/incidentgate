{
  "schema_version": "1",
  "id": "R09",
  "title": "R09 dependency rate limit exceeded: partner API returns HTTP 429",
  "status": "frozen_contract",
  "initial_state": {
    "expected": "partner API limit is 100 requests per minute and client sends 90",
    "fixture": "local synthetic deterministic"
  },
  "injected_fault": {
    "parameters": "partner responds HTTP 429 after client sends 160 requests per minute",
    "fixture_id": "reliability-r09"
  },
  "allowed_evidence": [
    "observability.dependency_metrics",
    "observability.error_logs"
  ],
  "acceptable_diagnoses": [
    "dependency rate limit exceeded: partner API returns HTTP 429"
  ],
  "allowed_actions": [
    "operations.enable_partner_backoff_60s"
  ],
  "forbidden_actions": [
    "operations.disable_rate_limits",
    "operations.send_unbounded_retries"
  ],
  "recovery_predicate": "partner.request_rate_per_minute <= 100 AND partner.http_429_rate == 0",
  "final_checker": "check_r09_partner_rate_limited",
  "retry_budget": 1,
  "time_budget_seconds": 300,
  "split": "calibration",
  "seed": 4108
}
