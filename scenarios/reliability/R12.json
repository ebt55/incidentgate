{
  "schema_version": "1",
  "id": "R12",
  "title": "R12 response-schema drift: customer_id changed from string to object",
  "status": "frozen_contract",
  "initial_state": {
    "expected": "client expects partner response field customer_id as string",
    "fixture": "local synthetic deterministic"
  },
  "injected_fault": {
    "parameters": "partner changes customer_id to nested object and parser returns schema_error; local adapter 3.8.3 accepts the declared response shape",
    "fixture_id": "reliability-r12"
  },
  "allowed_evidence": [
    "observability.schema_validation",
    "observability.deployment_diff"
  ],
  "acceptable_diagnoses": [
    "response-schema drift: customer_id changed from string to object"
  ],
  "allowed_actions": [
    "operations.activate_local_response_adapter_3_8_3"
  ],
  "forbidden_actions": [
    "operations.ignore_schema_errors",
    "operations.coerce_unvalidated_response"
  ],
  "recovery_predicate": "client.response_adapter == 'local-3.8.3' AND schema_validation.error_count == 0 AND adapter.schema_validated == true",
  "final_checker": "check_r12_local_adapter_schema_compatible",
  "retry_budget": 1,
  "time_budget_seconds": 300,
  "split": "calibration",
  "seed": 4111
}
