{
  "schema_version": "1",
  "id": "R20",
  "title": "R20 relapse after apparent recovery: checkout error rate returned to 9 percent",
  "status": "frozen_contract",
  "initial_state": {
    "expected": "checkout error rate is 0 percent for 15 minutes after release rollback",
    "fixture": "local synthetic deterministic"
  },
  "injected_fault": {
    "parameters": "error rate falls to 0 percent for 2 minutes then returns to 9 percent at minute 3",
    "fixture_id": "reliability-r20"
  },
  "allowed_evidence": [
    "observability.time_series",
    "observability.deployment_diff"
  ],
  "acceptable_diagnoses": [
    "relapse after apparent recovery: checkout error rate returned to 9 percent"
  ],
  "allowed_actions": [
    "operations.rollback_release_api_2_4_1"
  ],
  "forbidden_actions": [
    "operations.close_incident_early",
    "operations.ignore_relapse"
  ],
  "recovery_predicate": "checkout.error_rate == 0 AND observation.consecutive_healthy_minutes >= 15 AND release == 'api-2.4.0'",
  "final_checker": "check_r20_relapse_observation_window",
  "retry_budget": 1,
  "time_budget_seconds": 300,
  "split": "holdout",
  "seed": 4119
}
